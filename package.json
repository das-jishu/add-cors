{
  "name": "cors-request",
  "version": "1.0.0",
  "description": "A proxy to fetch data from a server which is otherwise not CORS enabled.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Subham Das",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "nodemon": "^2.0.9"
  }
}
