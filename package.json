{
  "name": "cors-request",
  "version": "1.0.0",
  "description": "A proxy to fetch data from a server which is otherwise not CORS enabled.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --config jestconfig.json --coverage",
    "build": "npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/das-jishu/add-cors.git"
  },
  "author": "Subham Das",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-rate-limit": "^5.3.0",
    "node-fetch": "^2.6.1",
    "nodemon": "^2.0.9"
  },
  "devDependencies": {
    "jest": "^27.0.6"
  }
}
